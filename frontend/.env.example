# Backend as published by docker-compose (backend-core maps 8002 -> 8000).
# Use port 8000 instead when running uvicorn directly on the host.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8002
NEXT_PUBLIC_WS_BASE_URL=ws://localhost:8002
